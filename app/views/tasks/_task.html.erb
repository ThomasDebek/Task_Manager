<div id="<%= dom_id task %>">
  <p>
    <strong>Title:</strong>
    <%= task.title %>
    – <%= task.completed ? "DONE" : "ACTIVE" %>
    <%= button_to "Toggle status", toggle_task_path(task), method: :patch %>

  </p>

  <p>
    <strong>Description:</strong>
    <%= task.description %>
  </p>

  <p>
    <strong>Completed:</strong>
    <%= task.completed %>
  </p>

</div>
